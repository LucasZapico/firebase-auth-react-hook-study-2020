import {
  FirebaseContext,
  useFirebaseValue,
  FirebaseProvider,
} from '../../context/firebase-context';
import Firebase from './firebase';

export default Firebase;

export { FirebaseContext, useFirebaseValue, FirebaseProvider };
